//This simple code converts the user input in hours to days.
//If a certain amount of hours aren't enough to make a day, it will also show them.
var hours = prompt();
var hour = parseFloat(horas);
var days = parseInt(hour/24); 
var daysandhours = parseInt(hour%24);
console.log(`${dias} dias e ${diasEHoras} horas`);
