#include <stdio.h>

int main()
{

    float temperature;
    char base, symbol, convert;
    
    scanf("%f %c %c %c", &temperature, &base, &symbol, &convert);
        if (base == "C" || base == "c"){
            if (convert == "F" || convert == "f"){
                
            }
        }
    printf("%f %c %c %c", temperature, base, symbol, convert);
    return 0;
}