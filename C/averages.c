#include <stdio.h>

int main(){

    float num, sum, counter, ahritmetic, harmonic, geometrico;
    while (counter < 10){
        scanf("%f", num);
        sum = sum + num;
        counter++;
    }
    ahritmetic = sum/10;
    
    return 0;
}